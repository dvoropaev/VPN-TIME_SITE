extends ../../layouts/_layout.pug

block vars
  - var basePath = '../'
  - var bodyClass = 'page-merch'
  - var currentPage = 'merch'

block title
  title merch

block content
  main
    .container
      .merch-hero
        .merch-hero__image

                               
    .box__btn
      a(href="https://t.me/vpn_time_pro_bot" class="btn btn-blue" target="_blank")
        span.icon.icon-telegram
        .text связаться с нами
